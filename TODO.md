- palette
    - add proper args for saving files
    - add version
- version all tools
- wttr.in
    - get times (sunset, sunrise, ...)
    - use times to set buckets - if wttr.in can't be pinged estimate

